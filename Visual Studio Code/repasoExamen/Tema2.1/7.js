const p = prompt("primer numero");
const s = prompt("segundo numero");
const p1 = parseInt(p);
const s1 = parseInt(s);
const suma = p1 + s1;
document.write("La suma de " + p + " y " + s + " es " + suma);
